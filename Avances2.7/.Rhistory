rangos_a_partir <- (max_price - min_price) / 3
round_rangos <- round(rangos_a_partir)
# Define the price categories
bajo <- min_price + round_rangos
medio <- bajo + round_rangos
alto <- medio + round_rangos
# Step 1: Initialize 'economico' to 0 for all rows
train$economico <- 0
train$medio <- 0
train$caro <- 0
# Step 2: Create the 'medio' and 'caro' binary variables
train$medio <- ifelse(train$SalePrice > bajo & train$SalePrice <= medio, 1, 0)  # Medium (medio)
train$caro <- ifelse(train$SalePrice > medio, 1, 0)          # Expensive (caro)
# Step 3: Now set 'economico' to 1 for prices <= 'bajo'
train$economico <- ifelse(train$SalePrice <= bajo, 1, train$economico)
price$category <- apply(price, 1, function(row) {
if (row["caro"] == 1) {
return("caro")
} else if (row["medio"] == 1) {
return("medio")
} else if (row["economico"] == 1) {
return("economico")
} else {
return(NA)
}
})
train$category <- apply(train, 1, function(row) {
if (row["caro"] == 1) {
return("caro")
} else if (row["medio"] == 1) {
return("medio")
} else if (row["economico"] == 1) {
return("economico")
} else {
return(NA)
}
})
head(train)
# Determinar el mejor modelo
# 10. Genere un buen modelo de regresión, use para esto la variable del precio de la casa
# directamente. Tunee el modelo
get_mode <- function(x) {
ux <- na.omit(unique(x))
ux[which.max(tabulate(match(x, ux)))]
}
# Convert factor columns to character first if needed
datos <- datos %>%
mutate(across(where(is.factor), as.character))
# Impute: mean for numeric, mode for character
datos <- datos %>%
mutate(across(where(is.numeric), ~ ifelse(is.na(.), mean(., na.rm = TRUE), .))) %>%
mutate(across(where(is.character), ~ ifelse(is.na(.), get_mode(.), .)))
# Convertir factores a texto si hace falta
datos <- datos %>%
mutate(across(where(is.factor), as.character))
# Reemplazar NAs
train <- train %>%
mutate(across(where(is.numeric), ~ ifelse(is.na(.), mean(., na.rm = TRUE), .))) %>%
mutate(across(where(is.character), ~ ifelse(is.na(.), get_mode(.), .)))
modeloCaret <- train(category~., data=train, method="nnet", trace=F)
test$prediccionCaret<-predict(modeloCaret, newdata = datos)
knitr::opts_chunk$set(echo = TRUE)
datos <- merge(datos, price)
head(datos)
test$prediccionCaret<-predict(modeloCaret, newdata = datos)
knitr::opts_chunk$set(echo = TRUE)
datos$prediccionCaret<-predict(modeloCaret, newdata = datos)
knitr::opts_chunk$set(echo = TRUE)
cfmCaret<-confusionMatrix(datos$prediccionCaret,datos$category)
cfmCaret
cfmCaret<-confusionMatrix(datos$prediccionCaret,datos$category)
View(train)
View(datos)
cfmCaret<-confusionMatrix(datos$prediccionCaret,datos$category)
knitr::opts_chunk$set(echo = TRUE)
niveles <- union(unique(datos$prediccionCaret), unique(datos$category))
# Convertir a factores con los mismos niveles
pred <- factor(datos$prediccionCaret, levels = niveles)
ref  <- factor(datos$category, levels = niveles)
# Matriz de confusión
cfmCaret <- confusionMatrix(pred, ref)
cfmCaret
knitr::opts_chunk$set(echo = TRUE)
modeloCaret <- train(category~., data=train, method="nnet", trace=F)
datos$prediccionCaret<-predict(modeloCaret, newdata = datos)
# Une los niveles posibles para realizar factor
niveles <- union(unique(datos$prediccionCaret), unique(datos$category))
pred <- factor(datos$prediccionCaret, levels = niveles)
ref  <- factor(datos$category, levels = niveles)
# Matriz de confusión
cfmCaret <- confusionMatrix(pred, ref)
cfmCaret
knitr::opts_chunk$set(echo = TRUE)
cfmCaret
knitr::opts_chunk$set(echo = TRUE)
NB <- make_Weka_classifier("weka/classifiers/functions/MultilayerPerceptron")
NB
WOW(NB)
nnodos='4'
knitr::opts_chunk$set(echo = TRUE)
datoscompletos -> rbind(datos, train)
knitr::opts_chunk$set(echo = TRUE)
datoscompletos <- rbind(datos, train)
NB <- make_Weka_classifier("wektrainNB <- make_Weka_classifier("weka/classifiers/functions/MultilayerPerceptron")
knitr::opts_chunk$set(echo = TRUE)
datoscompletos<- rbind(datos, train)
knitr::opts_chunk$set(echo = TRUE)
library(ggplot2)
library(rpart)
library(rpart.plot)
library(e1071)  # Para SVM
library(caret)  # Para evaluación de modelos y tuning
library(dplyr)  # Para manipulación de datos
library(caret)
library(nnet)
library(RWeka)
library(dummy)
library(neuralnet)
datos <-read.csv("test.csv")
price <- read.csv("sample_submission.csv")
# Calculate the min, max, and ranges
max_price <- max(price$SalePrice)
min_price <- min(price$SalePrice)
rangos_a_partir <- (max_price - min_price) / 3
round_rangos <- round(rangos_a_partir)
# Define the price categories
bajo <- min_price + round_rangos
medio <- bajo + round_rangos
alto <- medio + round_rangos
# Step 1: Initialize 'economico' to 0 for all rows
price$economico <- 0
price$medio <- 0
price$caro <- 0
# Step 2: Create the 'medio' and 'caro' binary variables
price$medio <- ifelse(price$SalePrice > bajo & price$SalePrice <= medio, 1, 0)  # Medium (medio)
price$caro <- ifelse(price$SalePrice > medio, 1, 0)          # Expensive (caro)
# Step 3: Now set 'economico' to 1 for prices <= 'bajo'
price$economico <- ifelse(price$SalePrice <= bajo, 1, price$economico)
head(price)
train <- read.csv("train.csv")
# Calculate the min, max, and ranges
max_price <- max(train$SalePrice)
min_price <- min(train$SalePrice)
rangos_a_partir <- (max_price - min_price) / 3
round_rangos <- round(rangos_a_partir)
# Define the price categories
bajo <- min_price + round_rangos
medio <- bajo + round_rangos
alto <- medio + round_rangos
# Step 1: Initialize 'economico' to 0 for all rows
train$economico <- 0
train$medio <- 0
train$caro <- 0
# Step 2: Create the 'medio' and 'caro' binary variables
train$medio <- ifelse(train$SalePrice > bajo & train$SalePrice <= medio, 1, 0)  # Medium (medio)
train$caro <- ifelse(train$SalePrice > medio, 1, 0)          # Expensive (caro)
# Step 3: Now set 'economico' to 1 for prices <= 'bajo'
train$economico <- ifelse(train$SalePrice <= bajo, 1, train$economico)
price$category <- apply(price, 1, function(row) {
if (row["caro"] == 1) {
return("caro")
} else if (row["medio"] == 1) {
return("medio")
} else if (row["economico"] == 1) {
return("economico")
} else {
return(NA)
}
})
train$category <- apply(train, 1, function(row) {
if (row["caro"] == 1) {
return("caro")
} else if (row["medio"] == 1) {
return("medio")
} else if (row["economico"] == 1) {
return("economico")
} else {
return(NA)
}
})
head(train)
# Determinar el mejor modelo
# 10. Genere un buen modelo de regresión, use para esto la variable del precio de la casa
# directamente. Tunee el modelo
get_mode <- function(x) {
ux <- na.omit(unique(x))
ux[which.max(tabulate(match(x, ux)))]
}
# Convert factor columns to character first if needed
datos <- datos %>%
mutate(across(where(is.factor), as.character))
# Impute: mean for numeric, mode for character
datos <- datos %>%
mutate(across(where(is.numeric), ~ ifelse(is.na(.), mean(., na.rm = TRUE), .))) %>%
mutate(across(where(is.character), ~ ifelse(is.na(.), get_mode(.), .)))
# Convertir factores a texto si hace falta
datos <- datos %>%
mutate(across(where(is.factor), as.character))
# Reemplazar NAs
train <- train %>%
mutate(across(where(is.numeric), ~ ifelse(is.na(.), mean(., na.rm = TRUE), .))) %>%
mutate(across(where(is.character), ~ ifelse(is.na(.), get_mode(.), .)))
datos <- merge(datos, price)
head(datos)
datoscompletos<- rbind(datos, train)
NB <- make_Weka_classifier("weka/classifiers/functions/MultilayerPerceptron")
NB
WOW(NB)
nnodos='4'
modelo.bp<-NB(Species~., data=datos,subset = corte, control=Weka_control(H=nnodos, N=1000, G=TRUE), options=NULL)
knitr::opts_chunk$set(echo = TRUE)
modelo.bp<-NB(Species~., data=datoscompletos, control=Weka_control(H=nnodos, N=1000, G=TRUE), options=NULL)
knitr::opts_chunk$set(echo = TRUE)
modelo.bp<-NB(category~., data=datoscompletos, control=Weka_control(H=nnodos, N=1000, G=TRUE), options=NULL)
knitr::opts_chunk$set(echo = TRUE)
nnodos=4
modelo.bp<-NB(category~., data=datoscompletos, control=Weka_control(H=nnodos, N=1000, G=TRUE), options=NULL)
knitr::opts_chunk$set(echo = TRUE)
modeloCaret <- train(category~., data=train, method="nnet", trace=F)
prediccionCaret<-predict(modeloCaret, newdata = datos)
# Une los niveles posibles para realizar factor
niveles <- union(unique(prediccionCaret), unique(datos$category))
pred <- factor(prediccionCaret, levels = niveles)
ref  <- factor(datos$category, levels = niveles)
# Matriz de confusión
cfmCaret <- confusionMatrix(pred, ref)
cfmCaret
knitr::opts_chunk$set(echo = TRUE)
datoscompletos<- rbind(datos, train)
train$y<-as.numeric(train$category)
datos$y<-as.numeric(datos$category)
modelo.nn <- neuralnet(y~Sepal.Length+Petal.Length, train[,c(1,3,6)], hidden = 2, rep = 3)
View(iris)
View(iris)
knitr::opts_chunk$set(echo = TRUE)
modelo.nn <- neuralnet(y~SalePrice+LotArea, train[,c(1,3,6)], hidden = 2, rep = 3)
porcentaje<-0.7
datos<-iris
set.seed(123)
corte <- sample(nrow(datos),nrow(datos)*porcentaje)
train<-datos[corte,]
test<-datos[-corte,]
#-------------------------------------------------
# Red Neuronal con NeuralNet
#-------------------------------------------------
train$y<-as.numeric(train$Species)
test$y<-as.numeric(test$Species)
modelo.nn <- neuralnet(y~Sepal.Length+Petal.Length, train[,c(1,3,6)], hidden = 2, rep = 3)
View(train[,c(1,3,6)])
knitr::opts_chunk$set(echo = TRUE)
modelo.nn <- neuralnet(y~SalePrice+LotArea, train[,c("SalePrice","LotArea","y")], hidden = 2, rep = 3)
View(train)
knitr::opts_chunk$set(echo = TRUE)
datos <-read.csv("test.csv")
price <- read.csv("sample_submission.csv")
# Calculate the min, max, and ranges
max_price <- max(price$SalePrice)
min_price <- min(price$SalePrice)
rangos_a_partir <- (max_price - min_price) / 3
round_rangos <- round(rangos_a_partir)
# Define the price categories
bajo <- min_price + round_rangos
medio <- bajo + round_rangos
alto <- medio + round_rangos
# Step 1: Initialize 'economico' to 0 for all rows
price$economico <- 0
price$medio <- 0
price$caro <- 0
# Step 2: Create the 'medio' and 'caro' binary variables
price$medio <- ifelse(price$SalePrice > bajo & price$SalePrice <= medio, 1, 0)  # Medium (medio)
price$caro <- ifelse(price$SalePrice > medio, 1, 0)          # Expensive (caro)
# Step 3: Now set 'economico' to 1 for prices <= 'bajo'
price$economico <- ifelse(price$SalePrice <= bajo, 1, price$economico)
head(price)
train <- read.csv("train.csv")
# Calculate the min, max, and ranges
max_price <- max(train$SalePrice)
min_price <- min(train$SalePrice)
rangos_a_partir <- (max_price - min_price) / 3
round_rangos <- round(rangos_a_partir)
# Define the price categories
bajo <- min_price + round_rangos
medio <- bajo + round_rangos
alto <- medio + round_rangos
# Step 1: Initialize 'economico' to 0 for all rows
train$economico <- 0
train$medio <- 0
train$caro <- 0
# Step 2: Create the 'medio' and 'caro' binary variables
train$medio <- ifelse(train$SalePrice > bajo & train$SalePrice <= medio, 1, 0)  # Medium (medio)
train$caro <- ifelse(train$SalePrice > medio, 1, 0)          # Expensive (caro)
# Step 3: Now set 'economico' to 1 for prices <= 'bajo'
train$economico <- ifelse(train$SalePrice <= bajo, 1, train$economico)
price$category <- apply(price, 1, function(row) {
if (row["caro"] == 1) {
return("caro")
} else if (row["medio"] == 1) {
return("medio")
} else if (row["economico"] == 1) {
return("economico")
} else {
return(NA)
}
})
train$category <- apply(train, 1, function(row) {
if (row["caro"] == 1) {
return("caro")
} else if (row["medio"] == 1) {
return("medio")
} else if (row["economico"] == 1) {
return("economico")
} else {
return(NA)
}
})
head(train)
# Determinar el mejor modelo
# 10. Genere un buen modelo de regresión, use para esto la variable del precio de la casa
# directamente. Tunee el modelo
get_mode <- function(x) {
ux <- na.omit(unique(x))
ux[which.max(tabulate(match(x, ux)))]
}
# Convert factor columns to character first if needed
datos <- datos %>%
mutate(across(where(is.factor), as.character))
# Impute: mean for numeric, mode for character
datos <- datos %>%
mutate(across(where(is.numeric), ~ ifelse(is.na(.), mean(., na.rm = TRUE), .))) %>%
mutate(across(where(is.character), ~ ifelse(is.na(.), get_mode(.), .)))
# Convertir factores a texto si hace falta
datos <- datos %>%
mutate(across(where(is.factor), as.character))
# Reemplazar NAs
train <- train %>%
mutate(across(where(is.numeric), ~ ifelse(is.na(.), mean(., na.rm = TRUE), .))) %>%
mutate(across(where(is.character), ~ ifelse(is.na(.), get_mode(.), .)))
datos <- merge(datos, price)
head(datos)
modeloCaret <- train(category~., data=train, method="nnet", trace=F)
datoscompletos<- rbind(datos, train)
train$y<-as.numeric(train$category)
datos$y<-as.numeric(datos$category)
modelo.nn <- neuralnet(y~SalePrice+LotArea, train[,c("SalePrice","LotArea","y")], hidden = 2, rep = 3)
knitr::opts_chunk$set(echo = TRUE)
train$y <- ifelse(train$category == "economico", 1, 0)
modelo.nn <- neuralnet(y~SalePrice+LotArea, train[,c("SalePrice","LotArea","y")], hidden = 2, rep = 3)
plot(modelo.nn, newdata=test) #Sale un gr?fico por cada repetici?n, en este caso saldr?n 3 gr?ficos
plot(modelo.nn, newdata=test) #Sale un gr?fico por cada repetici?n, en este caso saldr?n 3 gr?ficos
train$y<-as.numeric(train$category)
datos$y<-as.numeric(datos$category)
train$y <- ifelse(train$category == "economico", 1, 0)
modelo.nn <- neuralnet(y~SalePrice+LotArea, train[,c("SalePrice","LotArea","y")], hidden = 2, rep = 3)
plot(modelo.nn, newdata=test) #Sale un gr?fico por cada repetici?n, en este caso saldr?n 3 gr?ficos
test$predNeuralNet<-round(predict(modelo.nn,newdata = datos),0)
iris
View(datos)
knitr::opts_chunk$set(echo = TRUE)
datos$y <- ifelse(datos$category == "economico", 1, 0)
test$predNeuralNet<-round(predict(modelo.nn,newdata = datos[1:80]),0)
knitr::opts_chunk$set(echo = TRUE)
test$predNeuralNet<-round(predict(modelo.nn,newdata = datos[,1:80]),0)
knitr::opts_chunk$set(echo = TRUE)
test$predNeuralNet<-round(predict(modelo.nn,newdata = datos),0)
knitr::opts_chunk$set(echo = TRUE)
datos$predNeuralNet<-round(predict(modelo.nn,newdata = datos),0)
knitr::opts_chunk$set(echo = TRUE)
cfmNeuralNet<-confusionMatrix(as.factor(datos$predNeuralNet),as.factor(test$y))
View(datos)
cfmNeuralNet<-confusionMatrix(as.factor(datos$predNeuralNet),as.factor(test$y))
cfmNeuralNet
cfmNeuralNet<-confusionMatrix(as.factor(datos$predNeuralNet),as.factor(test$y))
knitr::opts_chunk$set(echo = TRUE)
cfmNeuralNet<-confusionMatrix(as.factor(datos$predNeuralNet),as.factor(datos$y))
cfmNeuralNet
knitr::opts_chunk$set(echo = TRUE)
datos <-read.csv("test.csv")
price <- read.csv("sample_submission.csv")
# Calculate the min, max, and ranges
max_price <- max(price$SalePrice)
min_price <- min(price$SalePrice)
rangos_a_partir <- (max_price - min_price) / 3
round_rangos <- round(rangos_a_partir)
# Define the price categories
bajo <- min_price + round_rangos
medio <- bajo + round_rangos
alto <- medio + round_rangos
# Step 1: Initialize 'economico' to 0 for all rows
price$economico <- 0
price$medio <- 0
price$caro <- 0
# Step 2: Create the 'medio' and 'caro' binary variables
price$medio <- ifelse(price$SalePrice > bajo & price$SalePrice <= medio, 1, 0)  # Medium (medio)
price$caro <- ifelse(price$SalePrice > medio, 1, 0)          # Expensive (caro)
# Step 3: Now set 'economico' to 1 for prices <= 'bajo'
price$economico <- ifelse(price$SalePrice <= bajo, 1, price$economico)
head(price)
train <- read.csv("train.csv")
# Calculate the min, max, and ranges
max_price <- max(train$SalePrice)
min_price <- min(train$SalePrice)
rangos_a_partir <- (max_price - min_price) / 3
round_rangos <- round(rangos_a_partir)
# Define the price categories
bajo <- min_price + round_rangos
medio <- bajo + round_rangos
alto <- medio + round_rangos
# Step 1: Initialize 'economico' to 0 for all rows
train$economico <- 0
train$medio <- 0
train$caro <- 0
# Step 2: Create the 'medio' and 'caro' binary variables
train$medio <- ifelse(train$SalePrice > bajo & train$SalePrice <= medio, 1, 0)  # Medium (medio)
train$caro <- ifelse(train$SalePrice > medio, 1, 0)          # Expensive (caro)
# Step 3: Now set 'economico' to 1 for prices <= 'bajo'
train$economico <- ifelse(train$SalePrice <= bajo, 1, train$economico)
price$category <- apply(price, 1, function(row) {
if (row["caro"] == 1) {
return("caro")
} else if (row["medio"] == 1) {
return("medio")
} else if (row["economico"] == 1) {
return("economico")
} else {
return(NA)
}
})
train$category <- apply(train, 1, function(row) {
if (row["caro"] == 1) {
return("caro")
} else if (row["medio"] == 1) {
return("medio")
} else if (row["economico"] == 1) {
return("economico")
} else {
return(NA)
}
})
head(train)
# Determinar el mejor modelo
# 10. Genere un buen modelo de regresión, use para esto la variable del precio de la casa
# directamente. Tunee el modelo
get_mode <- function(x) {
ux <- na.omit(unique(x))
ux[which.max(tabulate(match(x, ux)))]
}
# Convert factor columns to character first if needed
datos <- datos %>%
mutate(across(where(is.factor), as.character))
# Impute: mean for numeric, mode for character
datos <- datos %>%
mutate(across(where(is.numeric), ~ ifelse(is.na(.), mean(., na.rm = TRUE), .))) %>%
mutate(across(where(is.character), ~ ifelse(is.na(.), get_mode(.), .)))
# Convertir factores a texto si hace falta
datos <- datos %>%
mutate(across(where(is.factor), as.character))
# Reemplazar NAs
train <- train %>%
mutate(across(where(is.numeric), ~ ifelse(is.na(.), mean(., na.rm = TRUE), .))) %>%
mutate(across(where(is.character), ~ ifelse(is.na(.), get_mode(.), .)))
datos <- merge(datos, price)
head(datos)
modeloCaret <- train(category~., data=train, method="nnet", trace=F)
prediccionCaret<-predict(modeloCaret, newdata = datos)
# Une los niveles posibles para realizar factor
niveles <- union(unique(prediccionCaret), unique(datos$category))
pred <- factor(prediccionCaret, levels = niveles)
ref  <- factor(datos$category, levels = niveles)
# Matriz de confusión
cfmCaret <- confusionMatrix(pred, ref)
cfmCaret
train$y<-as.numeric(train$category)
datos$y<-as.numeric(datos$category)
train$y <- ifelse(train$category == "economico", 1, 0)
datos$y <- ifelse(datos$category == "economico", 1, 0)
modelo.nn <- neuralnet(y~SalePrice+LotArea, train[,c("SalePrice","LotArea","y")], hidden = 2, rep = 3)
plot(modelo.nn, newdata=test) #Sale un gr?fico por cada repetici?n, en este caso saldr?n 3 gr?ficos
datos$predNeuralNet<-round(predict(modelo.nn,newdata = datos),0)
cfmNeuralNet<-confusionMatrix(as.factor(datos$predNeuralNet),as.factor(datos$y))
cfmNeuralNet
s
View(datos)
knitr::opts_chunk$set(echo = TRUE)
train$y<-as.numeric(train$category)
datos$y<-as.numeric(datos$category)
View(datos)
knitr::opts_chunk$set(echo = TRUE)
traincategory <- as.factor(train$category)
train$y <- as.numeric(traincategory)
datoscategory <- as.factor(datos$category)
datos$y <- as.numeric(datoscategory)
modelo.nn <- neuralnet(y~SalePrice+LotArea, train[,c("SalePrice","LotArea","y")], hidden = 2, rep = 3)
modelo.nn <- neuralnet(y~SalePrice+LotArea, train[,c("SalePrice","LotArea","y")], hidden = 2, rep = 3)
plot(modelo.nn, newdata=test) #Sale un gr?fico por cada repetici?n, en este caso saldr?n 3 gr?ficos
datos$predNeuralNet<-round(predict(modelo.nn,newdata = datos),0)
cfmNeuralNet<-confusionMatrix(as.factor(datos$predNeuralNet),as.factor(datos$y))
cfmNeuralNet
knitr::opts_chunk$set(echo = TRUE)
plot(modelo.nn, newdata=datos) #Sale un gr?fico por cada repetici?n, en este caso saldr?n 3 gr?ficos
traincategory <- as.factor(train$category)
train$y <- as.numeric(traincategory)
datoscategory <- as.factor(datos$category)
datos$y <- as.numeric(datoscategory)
modelo.nn <- neuralnet(y~SalePrice+LotArea, train[,c("SalePrice","LotArea","y")], hidden = 2, rep = 3)
plot(modelo.nn, newdata=datos) #Sale un gr?fico por cada repetici?n, en este caso saldr?n 3 gr?ficos
datos$predNeuralNet<-round(predict(modelo.nn,newdata = datos),0)
cfmNeuralNet<-confusionMatrix(as.factor(datos$predNeuralNet),as.factor(datos$y))
cfmNeuralNet
plot(modelo.nn, newdata=datos) #Sale un gr?fico por cada repetici?n, en este caso saldr?n 3 gr?ficos
knitr::opts_chunk$set(echo = TRUE)
plot(modelo.nn, newdata=datos) #Sale un gr?fico por cada repetici?n, en este caso saldr?n 3 gr?ficos
plot(modelo.nn, newdata=datos) #Sale un gr?fico por cada repetici?n, en este caso saldr?n 3 gr?ficos
